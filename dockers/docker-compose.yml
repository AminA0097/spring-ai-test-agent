services:
  db:
    image: postgres:15
    container_name: agent-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin1425369
      POSTGRES_DB: agent
    ports:
      - "5433:5432"
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
